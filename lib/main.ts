import "./pagination.css";
import "./table.css";
export { DynamicTable } from "./components/Table";
export type { Header } from "./types";
export type { ColumnType } from "./types";

// Import the CSS files so they are included in the bundle
// Remove the CSS imports to allow developers to use their own styles
